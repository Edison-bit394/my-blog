# 环境变量模板：复制成 .env 使用（.env 不会被提交到 Git）
#
# SITE_URL  网站完整地址，必须以 / 结尾
# SITE_BASE 部署子路径，必须以 / 开头和结尾
#
# 三种情况的填法：
#
#   1) 用 GitHub Pages 免费地址（默认，不用填，会自动识别）
#      https://<用户名>.github.io/<仓库名>/  →  SITE_BASE=/<仓库名>/
#
#   2) 绑定了自定义域名（如 edison-bit394.online）
#      SITE_URL=https://edison-bit394.online/
#      SITE_BASE=/
#
#   3) 本地临时预览，随便什么都行，不影响
#
# 线上部署时不要靠这个文件——GitHub Actions 里用仓库变量 SITE_URL 传入，
# 见 .github/workflows/deploy.yml。
