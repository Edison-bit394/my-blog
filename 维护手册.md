# 网站维护手册

面向"我自己动手改网站"的完整流程。目标是：**你不需要懂 Astro，也能安全地加内容、改文字、发上线。**

配套文档：
- `DEPLOY.md` —— 首次上线、域名、GitHub Pages 配置（一次性的事）
- `content/README.md` —— 内容目录与字段约定
- `配DNS说明.md` / `上线清单.md` —— DNS 与上线检查

---

## 0. 先记住这张图

```
你改本地文件  →  git push  →  GitHub Actions 自动构建  →  线上更新（约 1 分钟）
```

三个必须记住的事实：

1. **只在本地改，线上不会变**。必须 `git push`。
2. **文件名 = 网址**。`content/blog/做一棵不成材的树.md` → `/blog/做一棵不成材的树/`
3. **`.md` 开头那段 `---` 包起来的是 frontmatter**，其中 `title` 和 `pubDate` 必填，
   少一个会让**整站构建失败**（不是只影响这一篇）。

---

## 1. 我该改哪个文件

| 我想做的事 | 改哪里 |
| --- | --- |
| 写/改一篇随笔 | `content/blog/` |
| 写/改工作经验、复盘 | `content/work/` |
| 写/改产品介绍 | `content/products/` |
| 写/改学习笔记 | `content/learn/` |
| 改站点名、作者名、简介、GitHub/邮箱 | `src/consts.ts` |
| 改"关于"页正文 | `src/pages/about.astro` |
| 改首页大标题、栏目介绍 | `src/pages/index.astro` |
| 改顶部导航（增删栏目） | `src/layouts/BaseLayout.astro` 的 `nav` 数组 |
| 改页脚版权/链接 | `src/layouts/BaseLayout.astro` |
| 改配色、字号、间距 | `src/styles/global.css` |
| 放图片、favicon、robots.txt | `public/` |

> `src/` 里是网站的**模板代码**，改错了整站会出问题；`content/` 里是**你自己的内容**，随便改。
> 不确定的时候，只动 `content/` 就够写很久了。

---

## 2. 写一篇新文章（推荐流程）

```powershell
cd 'D:\AI\workspace\thesis\my experience\site'

# ① 生成骨架（自动写好 frontmatter，draft: true）
npm.cmd run new -- blog "今天想明白的一件事"

# ② 用编辑器打开 content\blog\今天想明白的一件事.md 写正文
#    写完把 draft: true 改成 draft: false

# ③ 本地预览：浏览器打开 http://localhost:4321/
npm.cmd run dev

# ④ 满意后发布
git add -A
git commit -m "新增随笔《今天想明白的一件事》"
git push
```

四个栏目名分别是 `blog` / `work` / `learn` / `products`：

```powershell
npm.cmd run new -- work "某项目复盘"
npm.cmd run new -- learn "操作系统课程笔记"
npm.cmd run new -- products "产品名"
```

### frontmatter 速查

```yaml
---
title: "文章标题"              # 必填
description: "一句话摘要"       # 强烈建议填，影响 SEO 与列表页摘要
pubDate: 2026-09-15            # 必填，格式 YYYY-MM-DD
updatedDate: 2026-10-01        # 可选，修订日期
tags: [随笔, 生活哲学]          # 可选，会自动生成标签页 /blog/?tag=随笔
draft: false                   # true = 只在本地可见，线上不生成
featured: true                 # 可选，是否上首页精选
---
```

**各栏目额外字段**（不填也能构建，会有默认值）：

| 栏目 | 额外字段 |
| --- | --- |
| `work` | `company` / `role` / `period` |
| `learn` | `source` / `progress`（在读/已读完/待整理/已实践）/ `rating`（1–5） |
| `products` | `productName` / `status`（在售/内测/已上线/规划中/已下线）/ `link` / `highlights`（列表） |

> 上面这张表和 `src/content.config.ts` 是同一份约定的两种写法，改字段要两边一起改。

---

## 3. 用 Obsidian 写（把知识库接进来）

知识库 `vault/90-发布/` 是唯一会被网站读取的目录。在 Obsidian 里写完，一条命令同步过来：

```powershell
npm.cmd run publish:check   # 只体检，不写文件（推荐先跑）
npm.cmd run publish         # 真正同步
```

脚本会自动：校验 frontmatter（缺 `title`/`pubDate` 的文件会被拦下）、**强制 `draft: false`**、
去掉 UTF-8 BOM、提醒你网站上还挂着哪些已从知识库删掉的内容。

同步完照样 `git add -A && git commit && git push`。

---

## 4. 本地预览

```powershell
npm.cmd run dev        # http://localhost:4321/  改文件自动刷新
npm.cmd run build      # 真构建一次，和线上一样（草稿会被排除）
npm.cmd run preview    # 预览构建产物
```

`dev` 模式下 `draft: true` 的文章也看得见（方便边写边看）；`build` 会排除草稿。
**所以"本地能看到、线上没有"通常不是 bug，是草稿没改成 `false`。**

---

## 5. 发布与确认上线

```powershell
git add -A
git commit -m "改了什么就写什么"
git push
```

然后确认（约 1 分钟）：

1. 打开 <https://github.com/Edison-bit394/my-blog/actions> 看最新一条是不是绿色。
2. 打开 <https://edison-bit394.online/> ，用 `Ctrl+F5` 强制刷新（GitHub Pages 有 10 分钟缓存）。

红色（失败）时：**线上还是上一版，不会半坏不坏**，改完再 push 一次即可。

---

## 6. 改错了怎么回退

| 情况 | 做法 |
| --- | --- |
| 还没 commit | `git checkout -- 文件路径` 丢弃改动 |
| 已 commit 没 push | `git reset --soft HEAD~1` 撤销提交，改动回到工作区 |
| 已 push 且想撤回 | `git revert <提交id>` 再 push（生成一个反向提交，最安全） |
| 只想让某篇文章下线 | 把它的 `draft` 改成 `true` 再 push |
| 想彻底删掉某篇 | 删除对应 `.md` 再 push（文件名删了，网址就 404） |

查看历史与每次改了什么：

```powershell
git log --oneline -10
git show <提交id> --stat
```

---

## 7. 常见故障对照表

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 本地改了内容，`npm run dev` 看不到变化 | Astro 内容缓存认为文件没变 | 删 `node_modules\.astro\data-store.json` 后重启 dev |
| 文章页只有标题日期、**正文空白** | 自定义 loader 没输出渲染结果（历史上踩过一次） | 确认 `src/loaders/markdown-dir.ts` 里有 `renderMarkdown(...)` 且 `store.set({ rendered })` |
| 草稿文章的网址线上还能打开 | 详情页路由没过滤草稿 | 确认 `src/pages/*/[slug].astro` 的 `getStaticPaths()` 里有 `!isProd \|\| !entry.data.draft` |
| 构建报 `require is not defined` | 用回了官方 `glob()` loader | **不要改回 `glob()`**，继续用 `src/loaders/markdown-dir.ts` |
| 源文件删了，页面还在生成 | `src/` 下留了 `content.config.ts.bak` 之类 | 备份文件放到项目**外面**，`npm run preflight` 会拦住 |
| 构建报 YAML / frontmatter 解析错误 | 文件带了 UTF-8 BOM 或缺 `title`/`pubDate` | 跑 `npm.cmd run preflight` 看具体是哪个文件 |
| `npm install` 或构建报 `EPERM` | 沙箱禁止子进程（**仅 DSH 内运行时**） | 让 DSH 放开权限执行；你手动开终端跑则无此问题 |
| `git push` 报 `schannel: AcquireCredentialsHandle failed` | 同上，沙箱禁止访问 Windows 凭据 | 同上；或在普通 PowerShell 窗口里自行 push |
| PowerShell 读文件中文乱码 | 老式编码识别 | 用 `Get-Content 文件 -Raw -Encoding utf8` |

发之前 30 秒自检：

```powershell
npm.cmd run preflight      # 检查 BOM / 残留文件 / frontmatter
npm.cmd run build          # 确认能构建
```

---

## 8. 命令速查

| 命令 | 作用 |
| --- | --- |
| `npm.cmd run dev` | 本地预览（含草稿），看 <http://localhost:4321/> |
| `npm.cmd run new -- blog "标题"` | 新建文章骨架 |
| `npm.cmd run publish:check` | 只体检知识库同步，不写文件 |
| `npm.cmd run publish` | 从 `vault/90-发布` 同步内容 |
| `npm.cmd run preflight` | 构建前防呆检查 |
| `npm.cmd run build` | 真构建（排除草稿） |
| `npm.cmd run verify:loader` | 校验内容路径契约（Linux CI 相关） |
| `git add -A; git commit -m "..."; git push` | 发布上线 |

> Windows 上统一用 `npm.cmd`，因为 PowerShell 默认禁止运行 `npm.ps1`。

---

## 9. 内容纪律（写给自己）

- **只写可公开的信息**：公司产品不写未公开规划、客户名单、内部数据。
- 脱敏写法：用「某客户」「约 XX%」替代具体名称和数字。
- 一篇文章解决一个问题，宁可写短。
- 网站的价值不在主题好不好看，而在**有没有持续在写**。
