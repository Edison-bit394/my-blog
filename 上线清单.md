# 上线清单（针对 edison-bit394.online）

按顺序做完这 5 步即可。**顺序很重要**，跳步会导致网站样式全丢。

---

## ✅ 已经完成的（我做的）

| 项目 | 状态 |
| --- | --- |
| Git 安装 | ✅ `D:\AI\Git`，版本 2.55.0 |
| Git 身份 | ✅ `Edison Hu` / `286030663@qq.com` |
| GitHub 账号 | ✅ `Edison-bit394` 存在（建于 2026-09-13） |
| GitHub 仓库 | ✅ `Edison-bit394/my-blog` 存在，公开，空的 |
| 远程地址 | ✅ 已从占位符 `YOURNAME` 修正为真实地址 |
| 网站配置 | ✅ 网址自动识别，已按你的域名生成正确的 canonical / sitemap / RSS |
| 本地提交 | ✅ 2 个提交，44 个文件 |
| `.env` 保护 | ✅ 确认未被提交（里面有域名配置） |

---

## ⏳ 需要你做的（5 步）

### 第 1 步：配 DNS 解析（在 DNSPod / 腾讯云）

你的域名 NS 指向 `eleanore.dnspod.net` / `word.dnspod.net`，
所以去**腾讯云 DNSPod 控制台** → 找到 `edison-bit394.online` → **记录管理** → 添加记录。

需要加 **5 条**记录：

| 主机记录 | 记录类型 | 记录值 | 说明 |
| --- | --- | --- | --- |
| `@` | A | `185.199.108.153` | 裸域名（不带 www） |
| `@` | A | `185.199.109.153` | |
| `@` | A | `185.199.110.153` | |
| `@` | A | `185.199.111.153` | |
| `www` | CNAME | `Edison-bit394.github.io` | 带 www 的地址 |

> 四条 A 记录是 GitHub Pages 的官方 IP，全部要加，作用是容灾。
> CNAME 的记录值**不要**带 `https://`，结尾也不要加点。

**为什么必须现在做**：网站配置里的网址已经改成你的域名了，
如果 DNS 不通就推送，网站会打不开（样式、导航全部 404）。

### 第 2 步：设置 GitHub 仓库变量（很容易漏）

打开 <https://github.com/Edison-bit394/my-blog/settings/variables/actions>

点 **New repository variable**，添加两个：

| Name | Value |
| --- | --- |
| `SITE_URL` | `https://edison-bit394.online/` |
| `SITE_BASE` | `/` |

> 注意是 **Variables** 不是 Secrets（这两项不是密码，不需要加密）。
> 漏了这步的话，构建会按 GitHub Pages 免费地址生成链接，绑了域名后样式会全丢。

### 第 3 步：推送代码

在终端执行（**新开一个窗口**，确保 Git 已生效）：

```powershell
cd "D:\AI\workspace\thesis\my experience\site"
git push -u origin main
```

第一次会弹出浏览器要求登录 GitHub，点授权即可。

### 第 4 步：打开 GitHub Pages 并绑定域名

打开 <https://github.com/Edison-bit394/my-blog/settings/pages>

1. **Build and deployment → Source** 选 **GitHub Actions**（不要选 Deploy from a branch）；
2. **Custom domain** 填 `edison-bit394.online`，点 **Save**；
3. 等 DNS 生效（几分钟到几小时，DNSPod 通常很快）；
4. DNS 通了之后，勾选 **Enforce HTTPS**（证书自动签发，可能要等几分钟）。

### 第 5 步：验证

```powershell
# DNS 是否生效（应返回 185.199.108~111.153 中的一个）
Resolve-DnsName edison-bit394.online -Type A

# 网站是否可访问
curl.exe -I https://edison-bit394.online/
```

浏览器打开 <https://edison-bit394.online/>，能看到首页、样式正常、点导航不 404，就算成功了。

---

## 顺序错了会怎样

| 情况 | 结果 |
| --- | --- |
| 没配 DNS 就推送 | 网站生成的是你域名的链接，但域名指向空 → 打不开 |
| 漏了第 2 步的仓库变量 | 构建按免费地址生成链接，绑域名后 CSS/导航全 404 |
| Pages 的 Source 选了 branch | 部署方式冲突，Actions 跑完了网站却不更新 |

**如果先推送了、之后才配 DNS**：等 DNS 生效并在第 4 步保存域名后，
去仓库 **Actions** 标签页点 **Re-run all jobs** 重新部署一次即可。

---

## 域名信息备查

| 项目 | 值 |
| --- | --- |
| 域名 | `edison-bit394.online` |
| 注册商 | 腾讯云 / DNSPod（据 NS 记录推断） |
| NS | `eleanore.dnspod.net`、`word.dnspod.net` |
| 当前解析 | 空（这就是域名现在打不开的原因） |
| GitHub Pages IP | `185.199.108.153` ~ `185.199.111.153` |
| Pages 目标 | `Edison-bit394.github.io` |
